# LaTeX build artifacts (keep sources + PDFs)
**/*.aux
**/*.log
**/*.out
**/*.toc
**/*.nav
**/*.snm
**/*.vrb
**/*.synctex.gz
**/*.fls
**/*.fdb_latexmk
**/*.lof
**/*.lot
**/*.bbl
**/*.blg

# Python / notebooks
**/__pycache__/
**/*.py[cod]
**/.pytest_cache/
**/.ipynb_checkpoints/

# Virtual envs / caches
**/.venv/
**/venv/
**/.cache/

# Local scratch/extractions (do not commit)
_tmp/
_tools/
build.ps1

# IDE / OS
**/.idea/
**/.vscode/
Thumbs.db
Desktop.ini
.DS_Store

# Copyrighted reference PDFs (keep local; do not commit)
Text-Books/

# Local datasets (keep small samples only; link large datasets)
datasets/
data/

# Quiz/viva (keep private; do not publish in public repo)
Unit-05/Lecture-03/quiz_student_aiken.txt
