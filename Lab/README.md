# Elements of AIML — Lab Experiments

Use the corresponding `notes.pdf` for each experiment (pre-submission handout).
After submission, use `reference.pdf` for cross-checking (shared only after submission).

## Experiment list

1. WEKA: Regression
2. WEKA: Classification
3. WEKA: Clustering
4. Pandas: Import/Export + basic summaries
5. Exploratory Data Analysis (EDA)
6. Missing values + outlier handling
7. Feature scaling + one-hot encoding
8. PCA (dimensionality reduction)
9. Handling imbalanced data
10. Data visualization (Matplotlib + Seaborn)
